- name: Install Apache
  apt:
    name: apache2
    state: present
    update_cache: yes

- name: Copy index.html to web root
  copy:
    src: index.html
    dest: /var/www/html/index.html
  notify: Restart Apache


